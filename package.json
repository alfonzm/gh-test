{
  "name": "gh-test",
  "version": "1.0.0",
  "description": "My flawless Nuxt.js project",
  "author": "Alfonz",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy:gh": "DEPLOY_ENV=GH_PAGES gh-pages -d dist",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/dotenv": "^1.4.0",
    "gh-pages": "^2.2.0",
    "nuxt": "^2.0.0"
  },
  "homepage": "https://alfonzm.github.io/gh-test",
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
